{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Harness pointer. Instructions live in AGENTS.md (imported via CLAUDE.md); skills live in .agents/skills (surfaced here through the .claude/skills symlink). Keep this file minimal — put shared config in the agnostic locations."
}
